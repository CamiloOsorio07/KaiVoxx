[build]
builder = "Nixpacks"

[build.nixpacks]
# Paquetes del sistema para audio
pkgs = [
  "ffmpeg",
  "libopus",
  "libogg",
]

[start]
# Ejecuta SOLO el bot JS (sin iniciar el bot Python)
cmd = "cd js-kaivoxx-bot && npm start"

