[build]
builder = "Nixpacks"

[build.nixpacks]
# Asegura un build 100% JS (incluye node/npm) para que NO intente usar Python.
pkgs = ["nodejs", "npm", "ffmpeg", "libopus", "libogg"]

[start]
# Ejecuta SOLO el bot JS
cmd = "cd js-kaivoxx-bot && npm start"

